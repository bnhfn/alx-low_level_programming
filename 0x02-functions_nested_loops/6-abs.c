#include <stdio.h>
#include "main.h"

/**
 * _abs - Function
 * Description: return abs value of a given number
 * Return: always 0 (success)
 * @int: store an integer value
*/

int _abs(int)
{
int x, y;
y = abs(x);

_putchar(y);

return (0);
}
