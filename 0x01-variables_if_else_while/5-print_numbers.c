#include<stdio.h>
/**
 * main - A program that prints all single digit number.
 * Description: Use printf.
 * Return: Always 0 (Success)
*/
int main(void)
{
int ch;
for (ch = 0; ch <= 9; ch++)
printf("%d\n", ch);
return (0);
}
